<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EmilysKnittingApp.Views.WelcomePage"
             xmlns:vm="clr-namespace:EmilysKnittingApp.ViewModels"
             Title="{Binding Title}">
    
    <!--
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    -->
 
 <!--
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">Pink</Color>
        </ResourceDictionary>
    </ContentPage.Resources>
 -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ScrollView>
            <StackLayout>
                <Image Grid.Row="0" Source="welcome.jpg"/>
                <Label Grid.Row="1" FontSize="32" TextColor="Black" Margin="10,0,10,0" Text="Welcome to KnittingApp"/>
                <Label Grid.Row="2" FontSize="18" TextColor="Black" Margin="10,0,10,0" Text="This is my text."/>
            </StackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
